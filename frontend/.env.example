# Amazon Ads Optimizer — Frontend Environment
# Copy to .env.local for local overrides.

# API base URL. Leave empty for local dev (Vite proxy forwards /api to localhost:8000).
# For production, set to your backend URL:
#   VITE_API_BASE_URL=https://amazonmcp-backend-production.up.railway.app/api
VITE_API_BASE_URL=
